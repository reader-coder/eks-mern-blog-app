# Base URL the React app uses to reach the backend API
# When running via docker-compose, replace 'localhost' with the backend
# service name if you're proxying server-side, otherwise keep localhost
# since this is used by the browser, not the container network.
VITE_API_URL=http://localhost:5000/api
